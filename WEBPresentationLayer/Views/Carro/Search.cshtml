@model IEnumerable<WEBPresentationLayer.Models.CarroListViewModel>
<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Placa)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.HorarioEntrada)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Placa)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.HorarioEntrada)
                    </td>
                    <td>
                        <li class="nav-item">
                            @Html.ActionLink("Registrar Saída", "InserirSaida", new { id=item.ID  }) |
                        </li>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>